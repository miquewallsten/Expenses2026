# Phase 2.7 — dev-only tooling, kept out of the runtime image.
#
# Install with:   pip install -r requirements-dev.txt
# CI uses this file; the production Dockerfile only installs requirements.txt.

-r requirements.txt

# Test runner
pytest==9.0.3
pytest-cov==5.0.0

# Lint / format / type-check
ruff==0.7.4
black==24.10.0
mypy==1.13.0
